// Bitwise left shift left shift operator using

#include<iostream>
using namespace std;

int main()
{
    int a,b;
    cout << "Enter the a : ";
    cin >> a;
    cout << "Enter the b : ";
    cin >> b;

    cout << (a << b) << endl;
    return 0;
}