#include<iostream>
#include<vector>
using namespace std;

int main()
{
    int n;
    cout << "Enter the size of the array : ";
    cin >> n;   
    int arr[n];
    cout << "Enter the elements of the array : ";
    for(int i=0;i<n;i++)ṇ
    {
        cin >> arr[i];
    }
    
    // sort 
    sort(arr,arr+n);
    
    // For frequency count
    int freq=1,
}